version: '3.8'

services:
  frontend:
    image: semi22/covoiturage:latest
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    image: backend:latest
    ports:
      - "4000:4000"
